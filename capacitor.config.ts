import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'emergency-camera',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
